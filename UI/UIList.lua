UIList = {
    Frame = unpack(require("UI.Frame")),

    BackgroundFrame = unpack(require("UI.BackgroundFrame")),
    ButtonFrame = unpack(require("UI.ButtonFrame")),
    HealthFrame = unpack(require("UI.HealthFrame")),
    ManaFrame = unpack(require("UI.ManaFrame")),
    RewardFrame = unpack(require("UI.RewardFrame")),
    SpellBar = unpack(require("UI.SpellBar")),
    SpellFrame = unpack(require("UI.SpellFrame")),
    TextFrame = unpack(require("UI.TextFrame")),
    TextureFrame = unpack(require("UI.TextureFrame")),
    UnitFrame = unpack(require("UI.UnitFrame")),
};

return UIList;